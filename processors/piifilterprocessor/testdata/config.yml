receivers:
  examplereceiver:

processors:
  hypertrace_piifilter:
    redaction_strategy: "hash"

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
