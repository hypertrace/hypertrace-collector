receivers:
  examplereceiver:

processors:
  hypertrace_piifilter:
    prefixes:
      - hypertrace.
    redaction_strategy: "hash"

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
