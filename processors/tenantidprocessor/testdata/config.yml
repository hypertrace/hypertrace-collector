receivers:
  examplereceiver:

processors:
  hypertrace_tenantid:
    tenantid_header_name: header-tenant
    tenantid_attribute_key: attribute-tenant

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
