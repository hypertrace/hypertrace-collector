receivers:
  examplereceiver:

processors:
  hypertrace_enduser:
    encoding: foo

exporters:
  exampleexporter:

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      exporters: [exampleexporter]
