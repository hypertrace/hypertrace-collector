receivers:
  zipkin:

processors:
  hypertrace_piifilter:

exporters:
  file:
    path: /var/log/hypertrace-collector.json

service:
  pipelines:
    traces:
      receivers:
        - zipkin
      processors:
        - hypertrace_piifilter
      exporters:
        - file
