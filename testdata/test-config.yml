receivers:
  zipkin:

processors:
  hypertrace_piifilter:

exporters:
  file:
    path: ./exported-data.json

service:
  pipelines:
    traces:
      receivers:
        - zipkin
      processors:
        - hypertrace_piifilter
      exporters:
        - file
